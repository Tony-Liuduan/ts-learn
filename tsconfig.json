{
    "compilerOptions": {
        "outDir": "./dist/", // 生成的所有文件放在dist目录下
        "sourceMap": false, // 使 TypeScript 生成 sourcemaps
        "noImplicitAny": true, // 当将 noImplicitAny 设置为 true，你必须为变量设置类型
        // use typescript to transpile jsx to js
        "jsx": "react", // "preserve", // 开启 JSX 模式, 但是 "preserve" 告诉 TypeScript 不要转换它(我们将使用 Babel)
        // 接受JavaScript做为输入
        "allowJs": true,
        // 默认地，TypeScript把null和undefined当做属于任何类型, 当启用了strictNullChecks，null和undefined获得了它们自己各自的类型null和undefined
        // 要当心，当你使用strictNullChecks，你的依赖也需要相应地启用strictNullChecks
        "strictNullChecks": true,
        "module": "commonjs", // 使用 ES2015 模块
        "target": "es5", // 编译成 es5 
        "strict": true
    },
    "include": [
        // 读取所有可识别的src目录下的文件
        // "./src/**/*"
        "./ts-demo/**/*"
    ],
    "exclude": [
        "node_modules"
    ]
}